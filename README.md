# AI Keyword Gap Analyzer 2025

**Công cụ SEO tự động phân tích khoảng trống từ khóa – dùng Gemini AI + SerpAPI**

---

## Chạy trên GitHub (không cần máy)

1. Vào tab **Actions**
2. Chọn **AI Keyword Gap Analyzer**
3. Nhập từ khóa + URL → **Run**

→ Tự động tạo `GAP_REPORT.xlsx` + `GAP_REPORT.json`

---

## Cài đặt local (nếu cần)

```bash
pip install requests pandas beautifulsoup4 google-generativeai python-dotenv openpyxl
